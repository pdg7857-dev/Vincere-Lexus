// Vincere Lexus CRM — Supabase connection for cross-device sync.
//
// Paste your project's values below (Supabase dashboard → Project Settings → API):
//   url     = "Project URL"        (e.g. https://abcxyz.supabase.co)
//   anonKey = "anon public" key    (safe to commit — it's public and gated by
//                                    Row Level Security; NEVER put service_role here)
//
// Leave them blank to run the app fully offline (localStorage only) — sync just
// stays off until these are filled in.
window.CRM_SUPABASE = {
  url: "",
  anonKey: ""
};
